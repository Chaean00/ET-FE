events {}

http {
    include /etc/nginx/mime.types; 
    default_type application/octet-stream;
    
    upstream et-backend {
        server ${VITE_API_BASE_URL}:${VITE_API_BASE_PORT};
    }

    server {
        listen 80;

        # 서버 이름 설정
        server_name ${VITE_API_BASE_URL};

        # 정적 파일의 기본 경로 설정
        root /usr/share/nginx/html;

        # SPA를 위한 기본 라우팅 설정
        location / {
            try_files $uri /index.html;
        }

        # 백엔드 (Express) API 요청 처리
        location /api/ {
            proxy_pass http://et-backend;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        ## sse api에 대한 요청도 처리해야 함.
        location /sse/ {
            proxy_pass http://et-backend;
            proxy_http_version 1.1;

            # SSE와 같이 장시간 연결을 유지하기 위해 버퍼링을 비활성화
            proxy_buffering off;
            proxy_cache off;

            # HTTP 헤더 설정 (필요에 따라 조정)
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            # 만약 백엔드에 keep-alive 헤더를 전달해야 한다면:
            proxy_set_header Connection "keep-alive";
        }
    }
}